# Copy to .env and override as needed. All vars are optional (safe defaults exist).
PII_ENVIRONMENT=development
PII_LOG_LEVEL=INFO

# Compliance weighting: cost of one false negative relative to one false positive.
PII_FN_FP_COST_RATIO=10
